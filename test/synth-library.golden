
   __                  __
  / /  ___ ___ ____   / /_
 / /__/ -_) _ `/ _ \_/ __/
/____/\__/\_,_/_//_/ \__/
Leant (Haskell) - a GHCi-style REPL for Lean 4.  Type :help for help, :quit to exit.
λ> :synth ((a → b) → List a → List b)
preparing synthesis environment (session imports + Lean)...
  it1  fun f x => List.map f x
  it2  fun f x => List.reverse (List.map f x)
  it3  fun f x => List.map (fun a => a) (List.map f x)
  it4  fun f x => List.map (fun a => a) (List.reverse (List.map f x))
  it5  fun f x => List.append (List.map f x) (List.map f x)
note: search truncated: candidate limit reached (60)
λ> :synth ((a → b → b) → b → List a → b)
  it1  fun _ x _ => x
  it2  fun f x y => List.foldr f x y
  it3  fun f x y => List.foldl (fun _ a => f a x) x y
note: search truncated: candidate limit reached (60)
λ> :synth (List (List a) → List a)
  it1  fun x => List.flatten x
  it2  fun x => List.reverse (List.flatten x)
  it3  fun x => List.map (fun a => a) (List.flatten x)
  it4  fun x => List.append (List.flatten x) (List.flatten x)
  it5  fun _ => .nil
note: search truncated: candidate limit reached (60)
λ> :synth (List a → List a → List a)
  it1  fun x _ => x
  it2  fun _ x => x
  it3  fun x _ => List.reverse x
  it4  fun x y => List.append x y
  it5  fun x y => List.append y x
note: search truncated: candidate limit reached (60)
λ> :synth (∀ a : Type, a → List a)
  it1  fun _ _ => .nil
note: search truncated: choice-point limit reached
λ> :synth ((List a → b) → a → b)
  it1  fun f _ => f (.nil)
note: search truncated: choice-point limit reached
λ> :set synth-library off
synth library: off
λ> :synth ((a → b) → List a → List b)
  it1  fun _ _ => .nil
λ> :quit
