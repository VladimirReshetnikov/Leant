# Further synthesis improvements: revised priorities

The next feature work is **Exference explicit Haskell kinds, integer indexing, then the original two-universe construction query**. The [dictionary-selection release](2026-09-14-dictionary-selection-acceptance.md) now completes its native and public gates and leaves the implementation queue. The subsequent [contextual proof-prefix repair](2026-09-14-contextual-proof-cutoff-acceptance.md) closes the observed default Haskell timeout. Further scheduling work requires new evidence.

This decision supersedes the execution order in the [previous delivery plan](2026-09-13-synthesis-delivery-retriage.md) and the earlier dictionary triage. The practical rank-N/impredicative goal remains active. Type-signature coverage, historical behavior receipts and current implementation acceptance are distinct obligations.

## Current status and evidence

The published [qualified-consumer repair](2026-09-14-contextual-polytype-acceptance.md) admits scoped query-supplied quantified types into Djinn's contextual choices. Its corrected isolated baseline compiles and produces no candidates; the repair passes eight direct/wrapped and first/alternative search variants, all **147 Djinn tests**, and both Haskell engines' public examples: **eight exact GHC compile/execution replays, four actual-False controls and two wrong-kind controls**. Djinn's public acceptance uses 4,096 choices. That archive retains the default unbounded one-shot timeout. The [prefix-checking follow-up](2026-09-14-contextual-proof-cutoff-acceptance.md) establishes its cause and closes the original default and wrapped public requests without changing limits: the contextual path was checking the whole lazy proof list before applying its cutoff. All 148 Djinn and 187 adapter tests pass, plus eight exact GHC replays, four actual-False and two wrong-kind controls across default/finite runs. Leant remains on the separately accepted native pin below.

The released Leant implementation combines that exact Djex revision, `9a2d59d958a60ff3b6899697a60b6985a5cf73a3`, with source-evidence propagation across dictionary application. The [earlier triage index](../../test-church/receipts/dictionary-integration-retriage-2026-09-14.json) records verified local receipt hashes and their scope. It is a compact historical index, **not a portable acceptance archive**. Its pending native/publication status is superseded by the [native acceptance report and complete archive](2026-09-14-dictionary-selection-acceptance.md).

| Gate on the integrated implementation | Current result |
| --- | --- |
| Strict native build, focused tests and emitted Lean serializer | Pass; **98 focused tests**. Current captured source and runtime hashes checked again at triage. |
| Original dictionary-selection example and boxed-payload transfer, Djinn/Exference/Both | **18/18 pass:** 12 exact Lean replays with empty axiom inventories and six actual-False controls. |
| Lexical context regression gate, all three engines | **27/27 pass:** 18 exact empty-axiom replays, three actual-False controls and six explicit higher-class-universe refusals. |
| Eight existing nominal/polymorphic families through the changed Djinn backend | **24/24 pass:** 16 exact empty-axiom replays and eight actual-False controls. The original simultaneous two-universe query is excluded and remains open. |
| Full 766-test native suite on this Djex revision | **766 pass.** This gate uses the integrated dependency and final captured source/runtime snapshot. |
| Native implementation, gitlink and complete evidence | Included in the [native release](2026-09-14-dictionary-selection-acceptance.md), with its source/runtime/replay archive. |

Together the three public gates contain **69 passing cells: 46 exact replays, 17 actual-False controls and six refusal controls**. This is affected-scope validation, not a rerun of every engine/family combination. The first 18-cell run predates maintenance of the standalone lexical harness; the recorded bridge verifies unchanged active helper definitions and runtime identities. The original combined controller remains failed overall because its old 39-cell regression matrix expected capabilities to be refused that newer releases support. The corrected 27-cell gate removes those obsolete expectations and preserves higher-class-universe refusals. Global-provider behavior remains engine/inventory dependent; Djinn acceptance does not establish Exference acceptance.

Earlier [nominal/polymorphic acceptance](2026-09-14-selected-polymorphic-types-acceptance.md) retains its own 95-focused/763-native/72-public/105-regression evidence. The [intrinsic-unit](2026-09-14-intrinsic-unit-construction-acceptance.md), [contextual-binder](2026-09-14-contextual-universe-acceptance.md), [strict-implicit](2026-09-13-strict-implicit-source-acceptance.md), product and Church-composition releases likewise leave the implementation queue. Preserve their relevant regressions when a new change can affect them; do not add historical suite counts together as current acceptance.

## Execution order

The original priorities 1–4 remain the delivery commitments. Their next actions change as evidence arrives.

| Order | Improvement and practical value | Next action and completion criterion |
| --- | --- | --- |
| **1: accepted; regression coverage** | **Dictionary-qualified polymorphic selection.** The demonstrated reconstruction and candidate-vocabulary failures now pass the complete scoped native/public release. | Preserve the 98-focused/766-native/69-public acceptance, failed old-harness attempt and helper-equivalence bridge. Repeat affected gates when new changes warrant it; do not restart the repaired investigation. |
| **1 follow-up: accepted; regression coverage** | **Default Djinn responsiveness.** The observed qualified-consumer timeout is repaired. | Contextual proof checking now respects its raw prefix. Preserve the failing qualified Cartesian baseline, bounded/unbounded prefix equivalence and original public GHC replays. Candidate limits, overflow lookahead, ranking and defaults are unchanged. Further scheduler work needs a different demonstrated failure. |
| **2: next feature** | **Exference explicit Haskell kinds.** This is a confirmed unsupported public entrance, independently useful across rank-N examples. | The adapter validates annotations and then deliberately returns `DJEX_EXF_SOURCE_KINDS`. Preserve lexical kind ownership through opening, alias expansion, freshening, substitution, selected arguments and provider checking. Close the first actual representation boundary before lifting the guard. Require Exference's own ordinary and named-`where` outputs on the existing kinded corpus, exact GHC replay, vacuous/shadowed binder cases and wrong-kind controls. Shared source transport and a kind-aware renderer are prerequisites, not backend acceptance. |
| **3: next behavior** | **Integer `at`.** It has a small explicit contract and five cells without indexed acceptance. | Establish one native baseline using the existing controller before inferring failure from historical Haskell results. Separate generic integer operations from construction of the fold's index state. Preserve source `Int` and the supplied default, including negative, zero, in-range and out-of-range observations. Require both Haskell engines and all three native modes with independent replay and real rejection controls. Neither a Nat-only replacement nor a supplied implementation closes the task. |
| **4: bounded diagnosis, repair conditional** | **Two distinct nominal universe selections in one result.** This tests composition that single and chained selections do not establish. | Retain the original `GenericBox.{0}` / `GenericBox.{1}` query and both payload observations. Trace provider discovery, application, product construction and pruning to the first missing step at unchanged limits. A generic-payload comparison can isolate a cause but cannot replace the original acceptance query. Promote a shared correctness repair immediately if demonstrated; otherwise keep unexplained search work behind items 2 and 3. Require individual engines and Both, exact replay and actual-False controls. |
| **5: focused source extension** | **Higher-universe classes and selected global providers.** This remains a practical source-language gap, but the local dictionary repair does not require a general discovery redesign. | Start with one discriminating class/provider case, then two universe selections with distinguishable dictionaries. Preserve declaration-local universe ownership, argument/result sorts and evidence identity. Promote only the minimum prerequisite if it blocks an earlier item. Treat Prop/Sort formation and additional level operations as separate checked extensions. |
| **6: trace before implementation** | **Lean Djinn `length`, reductions and extrema.** These remain Church obligations, with unsuccessful construction experiments already recorded. | Choose one representative whose next trace can distinguish an absent, pruned or unaffordable witness step. First record a baseline for an unindexed cell when that answers a new question. Do not repeat the failed direct/wrapped `length`, carrier-only, descending-head or use-all-inputs experiments without a different causal hypothesis. One general repair must earn one discriminating behavioral result before expanding the family. |

Use one heavy build/runtime owner. The responsiveness repair is complete for its tested signatures and no longer precedes Exference kinds. Likewise, the two-universe search miss does not invalidate passing reconstruction examples: its recorded failure occurs before those candidates reach reconstruction. No ordering change here waives acceptance of the original outstanding queries.

## Other ideas: promote, retain or defer

| Idea | Revised disposition and trigger |
| --- | --- |
| **Actionable failure diagnostics** | Promote within the active fix. Distinguish unsupported source, no candidate within limits, lost selection/evidence, reconstruction failure, behavioral mismatch and verifier timeout. Add the smallest structured trace that discriminates the next hypothesis; defer a dashboard or general tracing framework. |
| **Finite candidate coverage and prefix stability** | Treat as a regression requirement for every search change. The quantified-choice repair retains candidate/assignment caps; late supplied types can still lose opportunities under those caps. Test a discriminating fixture near an affected cap or with an irrelevant added type when changing that path. Track first useful result and charged work. Do not infer completeness from the eight passing isolated variants or increase caps without evidence. |
| **Conditional, derived and superclass evidence; distinct dictionaries with equal predicates** | Retain as a required practical obligation. Promote a minimal case when it exposes a failure on a committed source milestone. A successful simple local dictionary does not establish global or derived-instance search. Keep dictionary identity explicit. |
| **Coverage-led baseline selection** | Promote as part of the next family, not a separate full-corpus campaign. The 43 cells without indexed evidence are unknown outcomes, not 43 confirmed defects. Use one representative and the existing observation/replay infrastructure; expand after it passes or identifies a shared failure. Update the ledger only from qualifying receipts. |
| **Harness capability expectations** | Maintain with the implementation boundary. The obsolete 39-cell refusals demonstrate test drift, not a new synthesis defect. When retiring a refusal, identify the positive suite that owns that capability and preserve engine-specific refusals. Defer a new capability registry unless repeated drift justifies it. |
| **Supplied-fold trees** | Retain as a transfer test for a general construction repair. Neither providing a fold nor replaying a reference witness is live synthesis acceptance. Promote if the test exposes a shared missing construction rule. |
| **Caching, counterexample-guided refinement and extra verifier workers** | Defer until profiles show repeated checking or verification dominates a successful candidate-producing path. Current no-candidate failures do not establish such a bottleneck. Any cache must include lexical scope, selected types, dictionary identity and observations. |
| **Search heuristics and reduction templates** | Keep failed variants retired. More candidate output and using every input did not establish the required behavior. Prefer one trace-supported general construction change over an operation-specific template. |
| **Recursive/dependent synthesis redesign or a Lean-native search engine** | Defer. A required public example must first demonstrate the missing representation or construction rule. The present acceptance gaps do not by themselves justify replacing the backend architecture. |
| **Editor integration and broader platform work** | Later product work. The demonstrated default command timeout has its accepted focused repair; promote another usability issue when directly observed on a required query. |
| **More reports and repeated full matrices** | Maintenance only. Keep README status and one current plan accurate, preserve failed attempts, and produce the release evidence once. Documentation volume and unchanged repeated runs are not feature progress. |

## Acceptance and stopping rules

Selected polymorphic types must preserve lexical scope, capture avoidance, visibility and actual universe formation. Nominal/class identities must distinguish their structured declarations and normalized level selections; global providers must instantiate declaration-owned universes from source evidence. Printed binder names are not ownership evidence. Lean's predicative universe hierarchy remains authoritative even when the corresponding Haskell signature uses impredicativity.

Each new source gate must require the selection being tested, rather than merely admit an unrelated inhabitant. Exercise ordinary and named-function `where` queries, each individual engine and Both where supported, independent exact replay, and actual-False or malformed-source controls. A Both result cannot establish either individual engine's success. For partial Church functions, retain supplied defaults or inhabitance assumptions in Lean.

The [behavior ledger](../../test-church/behavior-ledger.md) is unchanged: **94 historical acceptances, 23 attempted cells without indexed acceptance and 43 without indexed evidence**. The 66 cells without indexed acceptance comprise 15 reductions, one Lean Djinn `length`, five integer-`at` and 45 extrema cells. They are evidence categories, not a current pass rate or a count of proven missing capabilities. The source obligations above are additional. The dictionary milestone closes no Church behavior cells.

For each implementation: establish the discriminating query, repair the first demonstrated loss, run the affected family and appropriate release checks, then publish a stable milestone to both repositories with a coherent native dependency pin. A bounded miss is inconclusive, not a proof of non-inhabitation. Stop a failed hypothesis when its discriminating experiment fails; require new evidence before spending another larger search budget.
