# Synthesis delivery re-triage after scoped-evidence diagnostics

The [behavioral provider-admission experiment](2026-09-13-behavioral-provider-admission.md) is complete: both numeric providers were admitted, but Lean Djinn `length` still missed at its original deadline. The scheduling patch was archived and removed from the validation checkout. The next substantive repairs are preservation of scoped type/dictionary selections and reusable accumulator support for supplied-default reductions. These address an observed evidence loss and the largest remaining behavior family. Further search-order changes require a causal trace from the exact failing request.

The [trailing-type renderer release](2026-09-13-trailing-type-witness-acceptance.md) and two extended-operation batches are already delivered. The [latest batch](2026-09-13-matching-extended-native-frontier.md) exposes two Djinn-only misses while Both passes all seven operations. This re-triage changes execution order within the original priorities 1–4; ordinary data, contextual providers, public query support, and practical behavior coverage remain required. It does not expand the completion claim or promote the pending constructor changes.

## Current evidence and release boundary

- The [Church behavior ledger](../../test-church/behavior-ledger.md) enumerates exactly 160 cells. Its selected receipts record **92 historical acceptances, 15 attempted cells without indexed acceptance, and 53 cells without indexed evidence**. These are not a current-revision pass rate, and missing indexed evidence does not mean never attempted.
- The six supplied-default selectors `head`, `last`, `fromJust`, `fromLeft`, `fromRight`, and `atKey` have recorded acceptance across both Haskell engines and Lean Djinn, Exference, and Both. The new Lean Djinn `last` renderer change now has a separate complete isolated release receipt; the earlier working-source receipt alone was insufficient. See the [selector report](2026-09-13-native-supplied-default-selectors.md) and [last/atKey report](2026-09-13-trailing-type-witness-frontier.md).
- The isolated renderer checkout uses committed Leant `d2e5473e1cbabfc36094cd84c651a9793e4fbc68` and clean Djex `c1ad560e106f59df07d1a32c3b51158ef749fc99`, with only the renderer delta and two new tests. The first attempt passed its strict build but only **705/707 unit tests** and stopped before later gates. After the ordering repair and justified rendering-contract updates, the second attempt passes **707/707**, all six live behavior cells and three False controls, and both 350-signature replay corpora.
- The two failures concern a newly added `Unit` variant in the provider-result rendering list and an existing three-lane, 36-variant expectation. The repair preserves established variants ahead of fallbacks within each lane and checks the existing 32-variant cap. An independent kernel check additionally rejects the invalid new provider proposal at its original target. The first failed attempt remains archived separately.
- The [constructor diagnostic](2026-09-13-contextual-constructor-frontier.md) remains **17/21 positives and 6/6 actual False controls**. Its four failures comprise a Djinn method/tail bounded miss, ambiguous scoped reconstruction in Djinn and Both, and a separate Exference bounded miss.
- The previous production integration remains revision-specific: [705 native unit tests and both 350-signature replays](2026-09-12-contextual-list-native-integration.md). It does not validate the dirty constructor sources; the renderer has its own new isolated receipt.

The isolated failure evidence is local at `C:/Leant-validation/trailing-type-witness/dist-newstyle/trailing-type-witness-isolated-v1/results.json` and `unit/unit.stdout.txt`. The unfiltered suite completed in 284.04 seconds. The controller records unchanged inputs. The new isolated acceptance archive includes this unsuccessful attempt and a separate complete passing run. Indexing its six behavior cells preserves the ledger counts because those cells already had working-source acceptance.

The [native reduction baseline](2026-09-13-native-reductions-baseline.md) now records nine misses across `foldl1`, `foldr1`, and `reduce` in all three Lean modes. Every query reached its original deadline; all three oracle preflights and three actual False controls passed. This narrows the evidence gap without adding acceptance. Diagnose carrier admission and candidate derivations before changing search policy.

## Latest scoped-evidence findings: provisional, not released

The isolated Djex worktree at `C:/Djex-validation/scoped-source-selections` now carries source binder openings, selected type arguments, and dictionary occurrences through lowering into the independently checked source graph. Batch and stream duplicate suppression also retain distinct selections when the printed expressions coincide. This is a correctness repair: erasing the evidence before comparison can discard a different implementation.

The latest strict build passes. Earlier snapshots passed 119 private graph tests and 514 shared synthesis tests; neither result is a fresh full-suite pass after the latest eta-preservation change. Public batch and stream synthesis each produced both global-provider selections, with independent GHC observations `(37,91)` and `(91,37)`. These focused results do not validate native Lean integration or close a Church cell.

The latest permanent public regression passes the global-provider case but fails the local rank-N callback case. Its GHC diagnostic is at the test invocation `candidate @Int @Bool provider`: the context-only parameter of `provider :: forall p. C p => Token` is ambiguous. A separate diagnostic enables `TypeAbstractions` and passes `(\ @p -> provider @p)`. The unchanged generated candidate body then compiles and returns `(37,91)` under the two type orders. Thus this failure does not establish a renderer defect. Correct the fixture's explicit polymorphic argument, then rerun both public regressions; the standalone replay does not replace that gate.

The local evidence is in `dist-newstyle/scoped-selections-build-v5.log`, `scoped-selections-public-regressions-v2.log`, `replay-scoped-query-v1.log`, and `retriage-local-callback-replay.hs` / `.log` under that isolated worktree. It is provisional local evidence, without a portable release receipt. Equal-predicate dictionary overlap and deeper quantified callback admission remain guarded. Distinct parameters instantiated with distinct types do not demonstrate arbitrary selection between two dictionaries for the same predicate; ordinary Haskell source cannot generally name those hidden dictionary slots.

## Delivery order and concrete exit criteria

| Order | Improvement | Evidence and expected reach | Acceptance boundary |
| --- | --- | --- | --- |
| 1 | Finish and validate scoped source-evidence preservation. | The repair is implemented in isolation and global selections survive public batch/stream synthesis and GHC replay. The remaining local regression currently fails in its invocation fixture. This work also supplies a candidate repair for the native contextual reconstruction failure. | Correct the explicit polymorphic test argument; rerun both public cases and full affected suites on frozen final sources. Check scope escape, missing vectors, impredicative results, rewrite preservation, and evidence-sensitive deduplication. Validate native integration with the exact dependency before promotion. Keep unsupported overlap/deeper-scope guards. |
| 2 | Diagnose reusable reduction carriers, then extend to extrema and supplied-fold trees. | All nine Lean reduction baselines miss at their original deadline. Reductions account for 15 outstanding cells and extrema for 45. An empty/nonempty accumulator is a plausible common ingredient; order-sensitive folds and tree traversals may additionally need function or product carriers. | Trace one actual failing reduction and determine whether a useful carrier is absent, delayed, or rejected. Add a general rule only for the observed gap. Validate reductions before extrapolating to extrema; check empty/default, order, ties, and full types for every operation/mode. Supplied-fold tree acceptance remains a separate gate. |
| 3 | Repair remaining bounded searches using exact derivation traces. | Djinn `length`, both Djinn `maybeEither` cells, native method/tail composition, and Exference's nested-context miss remain unresolved. Admitting zero/successor did not repair `length`; previous contextual ordering changes also failed. | Capture the exact inventory, instantiations, selected evidence, truncation, and verification outcomes at original bounds. Identify the specific derivation a change admits or preserves. Treat the failures separately until traces establish a common cause. Preserve same-environment False controls and independent replay. |
| 4 | Close ordinary-data and contextual-provider integration. | The constructor diagnostic passes 17/21 positives and 6/6 controls. Source-graph repair may address the Djinn/Both nested-context failures, but does not yet address the two bounded-search misses. Broader provider evidence remains outside the supported fragment. | Require the full 21-positive/six-control matrix, final native unit suite, and affected signature replays. Deliver derived methods, conditional/superclass evidence, equal-predicate handling, and deeper scopes as separately tested increments. An explicit dictionary representation needs a design before removing Haskell's hidden-dictionary selection guard. |
| 5 | Finish original public-interface and source-language obligations. | Applicable named-function `where` entrances, kinded Haskell binders, richer Lean universes, and remaining supplied-fold/tree coverage are still required. Their priority is below observed correctness and construction gaps, not outside the goal. | Use a concrete entrance/source coverage checklist and exact public commands. Keep Haskell kind evidence distinct from Lean universe evidence. Add positive and refusal cases for each newly admitted fragment; publish canonical Djex changes before validating the pinned Leant dependency. |

The bounded experiment has reached its stopping condition: both numeric providers were admitted and `length` still missed. Retain the result and move to evidence/accumulator work unless a trace identifies a specific additional repair. Provider-first scheduling must also be checked for starving a useful structural or smaller-provider search; merely moving the starvation elsewhere is insufficient.

The completed provider-admission diagnostic has a strict build result, an unsuccessful original query, and passing provider-free and same-environment provider-enabled False controls. All frozen inputs stayed unchanged. Its 14-line `Main.hs` diff is archived, not released, and the validation checkout has returned to published source. The root constructor changes are separate and remain unaccepted.

These are dependency priorities, not instructions to run competing heavy validations. Freeze inputs for each run and use one build/runtime owner. Keep newly accepted shared fixes separate from unfinished constructor and search experiments. The ledger and the accepted renderer need maintenance, not another dedicated redesign milestone.

## What remains in the 160-cell target

There are **68 cells without indexed acceptance**, divided as follows:

| Batch | Cells | Proposed handling |
| --- | ---: | --- |
| Lean Djinn `length` and `maybeEither` | 2 | The provider-first experiment admits both numeric providers but `length` still misses; trace candidate construction next. `maybeEither` reaches the choice-point limit with verification/graph failures requiring separate attribution. Both mode has actual acceptance for both. |
| Haskell Djinn `maybeEither` | 1 | Retain its recorded failed attempt; attribute the search/evidence boundary before choosing a repair. |
| Supplied-default `foldl1`, `foldr1`, and `reduce`, all five modes | 15 | All nine Lean baseline cells now have recorded deadline misses. Inspect their carrier/derivation traces and assess the six Haskell cells separately; preserve empty/default behavior and nonempty accumulator behavior. |
| Nine extrema operations, all five modes | 45 | Group comparator-based, projection-based, and paired results by actual dependencies. Exercise singleton, ties, order, and supplied defaults; record each operation/mode separately. |
| Supplied-default native-`Int` `at`, all five modes | 5 | Keep distinct from accepted `atKey`. Preserve the declared integer primitive, negative/out-of-range behavior, and full type. Both Haskell modes already have attempts without indexed acceptance. |

The nine extrema operations are `maximumBy`, `maximumOn`, `minimumBy`, `minimumOn`, `minMaxBy`, `minmaxElement`, `maximum`, `minimum`, and `minMax`. Existing attempts and receipts determine implementation work; absence from this index alone does not establish a missing capability.

## Other ideas reconsidered

| Idea | Decision | Promotion trigger |
| --- | --- | --- |
| More coverage-ledger machinery | Maintain, do not expand by default. | A concrete missing provenance or outcome distinction. The 160-key index and 15 ledger tests are already delivered. |
| Source-selected type arguments; failure-directed rendering | Promote evidence preservation; defer general retry machinery. | The resolved renderer regressions show why fallback ordering and existing variant budgets require explicit tests. Use actual binder/kind/universe evidence where available; consider diagnostic-directed retries only with preserved candidate identity and limits. Do not build a general retry framework to fix one case. |
| Exact request capture and candidate accounting | Promote as focused diagnostics. | Reproduce the native method/tail failure with identical providers, assignments, strategy, and limits; identify derivation loss, truncation, duplicate work, or reconstruction refusal. Simplified passing fixtures are insufficient. |
| Search-order or derived-value redesign | Defer until a causal trace. | Earlier priority and checked-derived-value experiments failed the real 32-candidate fixture. Both experiments were reverted; retained logs are diagnostic evidence. Show which missing branch a new rule recovers at unchanged bounds. |
| Clear unsuccessful-query explanations | Add alongside the touched failure path. | Distinguish unsupported source, bounded exhaustion, reconstruction failure, and verification failure. Never report a bounded miss as impossibility. |
| Duplicate suppression and observation caching | Split correctness from optimization. | Preserve distinct selected type/dictionary evidence now: public synthesis exposed loss under expression-only deduplication. Defer general caching until repeated costly work is measured; cache identity must also include scope, environment, and observations. |
| Provider admission versus broader discovery/ranking | Diagnostic complete; defer another scheduling change. | Both useful providers were admitted and the original query still missed. A new trace must identify a missing or delayed useful derivation and justify the policy without starving structural search. Larger inventories alone have no demonstrated benefit. |
| More verifier workers, transport tuning, general memoization | Defer; record failure counts in the focused traces. | A measured bottleneck and an end-to-end improvement without weakening replay or process isolation. |
| General recursion/induction, dependent or indexed synthesis, a Lean-native engine | Future scope. | A required practical example that existing representations and supplied folds cannot express, with termination and equality obligations identified. |
| Editor integration and platform tooling | Later product work. | Stable command behavior and materially broader accepted practical coverage. |

## Completion and publication

The practical target remains **(13 extended + 19 supplied-default operations) × (2 Haskell engines + 3 Lean modes) = 160 behavior cells**, with controls and independent replay additional. Lean counterparts of partial functions retain the agreed supplied default or inhabitance assumption. Named-function-plus-`where` remains the behavioral query interface.

The 160 cells are a regression target, not a mathematical completeness theorem for arbitrary rank-N or impredicative inhabitation. Source-evidence, ordinary-data, contextual-provider, and public-query obligations remain part of the original goal even after the matrix passes.

Keep root READMEs current and push stable milestones to `origin/main` in both repositories. The isolated renderer repair is released with its complete evidence. This re-triage is a documentation milestone: the focused diagnostic adds no release acceptance or Church ledger cells. Scoped-evidence and contextual-constructor changes remain unaccepted implementation work; the provider-admission patch is archived and removed. Leant's production dependency is unchanged. Complete one bounded implementation milestone at a time, publish its exact validation, and then advance to the next independent construction gap.
